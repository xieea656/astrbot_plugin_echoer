{
  "scope": {
    "type": "object",
    "description": "记忆作用域 —— 决定哪些会话共享同一份记忆",
    "hint": "作用域模式影响记忆的隔离粒度，请根据实际场景选择。",
    "items": {
      "mode": {
        "type": "string",
        "description": "作用域隔离模式",
        "hint": "platform_global：同平台所有会话共享记忆；user_global：按用户隔离；group_global按群隔离，私聊自动退化为按用户隔离；umo：按最细粒度隔离，每个独立消息来源一份记忆。",
        "default": "group_global",
        "options": ["platform_global", "user_global", "group_global", "umo"]
      }
    }
  },
  "ingest": {
    "type": "object",
    "description": "消息写入策略 —— 控制哪些消息会被记录到记忆系统",
    "hint": "决定了插件会捕获哪些消息作为记忆素材。合理的过滤配置可以减少无效记忆噪声，提升检索质量。",
    "items": {
      "record_all_events": {
        "type": "bool",
        "description": "记录所有消息事件",
        "hint": "开启后，插件会监听并记录所有经过 AstrBot 的消息事件。关闭则仅记录被 AstrBot 处理的消息。",
        "default": true
      },
      "skip_empty_text": {
        "type": "bool",
        "description": "跳过空文本消息",
        "hint": "开启后会忽略纯图片、表情等不含文字内容的消息，避免写入无意义的空记录。",
        "default": true
      },
      "skip_command_messages": {
        "type": "bool",
        "description": "跳过命令消息",
        "hint": "开启后，以命令前缀（如 /、! 等）开头的消息不会被写入记忆，避免将指令本身作为记忆内容。",
        "default": true
      },
      "command_prefixes": {
        "type": "list",
        "description": "命令前缀列表",
        "hint": "定义哪些字符开头的消息被视为命令。消息前置 @机器人 后仍会检查这些前缀。示例：[\"/\", \"!\", \".\"]。",
        "default": ["/"]
      }
    }
  },
  "provider": {
    "type": "object",
    "description": "LLM 模型选择 —— 指定用于总结和画像生成的聊天模型",
    "hint": "Memorix 的总结生成、人物画像提取等功能需要调用大语言模型。留空使用当前会话默认的 Provider。",
    "items": {
      "chat_provider_id": {
        "type": "string",
        "description": "聊天模型 Provider ID（留空则使用当前会话的默认模型）",
        "hint": "插件的总结、画像生成等 LLM 任务将使用此模型。",
        "default": "",
        "_special": "select_provider",
        "obvious_hint": true
      }
    }
  },
  "embedding": {
    "type": "object",
    "description": "向量嵌入（Embedding）—— 控制文本向量化方式，直接影响语义检索质量",
    "hint": "Embedding 将文本转换为向量，是语义检索的核心。关闭时使用本地确定性向量回退，开启后使用 OpenAI 兼容服务，检索效果会显著提升。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用远程 Embedding 服务",
        "hint": "关闭（默认）：使用本地确定性向量回退，但语义检索精度有限。开启：调用你配置的 OpenAI 兼容 API，检索质量会大幅提升。开启前请确保下方的 API 地址和密钥已正确填写。",
        "default": false,
        "obvious_hint": true
      },
      "dimension": {
        "type": "int",
        "description": "向量维度",
        "hint": "Embedding 模型输出的向量维度。必须与你使用的 Embedding 模型匹配，否则会导致索引错误。若不确定请查阅模型文档。",
        "default": 1024
      },
      "batch_size": {
        "type": "int",
        "description": "单次批量请求的文本条数",
        "hint": "每次向 API 发送的文本数量上限。增大可提升写入速度，但会增加单次请求耗时和内存占用。建议范围 8~64。",
        "default": 32
      },
      "max_concurrent": {
        "type": "int",
        "description": "最大并发请求数",
        "hint": "同时向 API 发出的最大并发请求数。增大可加速批量处理，但可能触发 API 限流。",
        "default": 5
      },
      "openapi": {
        "type": "object",
        "description": "OpenAI 兼容 Embedding API 端点配置",
        "hint": "配置 OpenAI 兼容的 Embedding 服务地址、密钥和模型。。",
        "items": {
          "base_url": {
            "type": "string",
            "description": "API 地址（Base URL）",
            "hint": "Embedding API 的基础地址。可以不带末尾的 /v1，插件会自动补全。",
            "default": ""
          },
          "api_key": {
            "type": "string",
            "description": "API 密钥",
            "hint": "API Key",
            "default": ""
          },
          "model": {
            "type": "string",
            "description": "Embedding 模型名称",
            "hint": "要使用的 Embedding 模型名。",
            "default": ""
          },
          "timeout_seconds": {
            "type": "float",
            "description": "请求超时（秒）",
            "hint": "单次 Embedding API 请求的超时时间。",
            "default": 30.0
          },
          "max_retries": {
            "type": "int",
            "description": "请求失败重试次数",
            "hint": "单次 Embedding 请求失败后的最大重试次数。设为 0 则不重试。建议 2~5 次。",
            "default": 3
          }
        }
      }
    }
  },
  "retrieval": {
    "type": "object",
    "description": "记忆检索 —— 控制从记忆库中召回相关内容的方式和数量",
    "hint": "检索配置影响 LLM 接收到的上下文质量。合理的参数能在召回率和精度之间取得平衡。",
    "items": {
      "top_k_final": {
        "type": "int",
        "description": "最终返回的记忆条数",
        "hint": "每次 LLM 请求时，最终注入到上下文中的记忆条数上限。数值越大上下文越丰富，但会消耗更多 Token。建议范围 3~20。",
        "default": 10
      },
      "enable_ppr": {
        "type": "bool",
        "description": "启用 PageRank 图谱重排",
        "hint": "开启后，检索结果会经过 Personalized PageRank 算法基于知识图谱的拓扑结构进行二次重排，能提升关联性强的记忆的排名。纯算法优化。",
        "default": true
      },
      "enable_parallel": {
        "type": "bool",
        "description": "启用并行检索",
        "hint": "开启后，向量检索和稀疏检索（BM25）将并行执行，可降低检索延迟。",
        "default": true
      },
      "temporal": {
        "type": "object",
        "description": "时序检索 —— 按时间范围查询记忆",
        "hint": "时序检索允许用户通过 /mem time 命令按时间范围查询记忆，支持自然语言时间表达。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用时序检索功能",
            "hint": "关闭后 /mem time 命令将不可用。",
            "default": true
          },
          "default_top_k": {
            "type": "int",
            "description": "时序检索默认返回条数",
            "hint": "通过 /mem time 命令查询时，未指定数量时默认返回的记忆条数。建议范围 5~30。",
            "default": 10
          }
        }
      },
      "auto_route": {
        "type": "object",
        "description": "自动检索路由 —— 根据提问内容自动选择普通检索、时间检索或混合检索",
        "hint": "开启后，插件会在 LLM 注入记忆前先判断当前问题是否包含明确时间意图。纯回忆问题走时间检索，时间+主题同时存在时走混合检索，其余保持普通语义检索。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用自动检索路由",
            "hint": "关闭后将始终按普通语义检索处理，不再自动切换到 time/hybrid。",
            "default": true
          },
          "enable_time_intent": {
            "type": "bool",
            "description": "启用自然语言时间意图识别",
            "hint": "开启后，可识别“昨天”“上周”“这个月”“从昨天到今天”等常见中文时间表达，并自动构造时间窗口。",
            "default": true
          }
        }
      }
    }
  },
  "memory": {
    "type": "object",
    "description": "记忆生命周期 —— 控制记忆的自动衰减、冻结、剪枝和回收机制",
    "hint": "Memorix 的记忆不是永久存储，而是有生命周期的：活跃记忆会按半衰期自动衰减权重，低于阈值的进入冻结期等待唤醒，最终未被唤醒的将被剪枝回收。这套机制让记忆系统自动保留重要内容、淘汰过时信息。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用记忆自动维护",
        "hint": "关闭后记忆将永不衰减和剪枝，所有记录会永久保留。长期运行可能导致记忆库膨胀。建议保持开启。",
        "default": true
      },
      "half_life_hours": {
        "type": "float",
        "description": "记忆半衰期（小时）",
        "hint": "记忆关系权重衰减到一半所需的时间。值越大记忆保持越久，值越小遗忘越快。例如 24 表示一条记忆在 24 小时后权重降为原来的一半、48 小时后降为 1/4。建议范围 6~168。",
        "default": 24.0
      },
      "base_decay_interval_hours": {
        "type": "float",
        "description": "衰减执行周期（小时）",
        "hint": "系统多久执行一次衰减计算。值越小衰减越平滑但 CPU 开销更大。建议范围 0.5~6 小时。",
        "default": 1.0
      },
      "prune_threshold": {
        "type": "float",
        "description": "剪枝阈值",
        "hint": "当记忆关系权重衰减到低于此值时，该关系将被移入回收站。范围 0~1，值越大剪枝越激进（更多记忆会被回收），值越小保留越多。默认 0.1 表示权重低于 10% 时剪枝。",
        "default": 0.1
      },
      "freeze_duration_hours": {
        "type": "float",
        "description": "冻结保留时长（小时）",
        "hint": "记忆降至不活跃状态后，在被剪枝前会进入「冻结」等待期。在此期间如果被再次访问可以「复活」。值越大给予记忆更多唤醒机会。建议范围 6~72 小时。",
        "default": 24.0
      },
      "max_weight": {
        "type": "float",
        "description": "关系边最大权重",
        "hint": "单条记忆关系的权重上限，防止被频繁强化的记忆权重无限增长。建议范围 5~20。过低会限制核心记忆的突出度，过高则衰减周期会拉长。",
        "default": 10.0
      },
      "auto_protect_ttl_hours": {
        "type": "float",
        "description": "强化自动保护时长（小时）",
        "hint": "通过 /mem reinforce 强化记忆后，该记忆在此时长内不会被衰减或剪枝。建议范围 6~168 小时。",
        "default": 24.0
      },
      "orphan": {
        "type": "object",
        "description": "孤儿节点回收 —— 管理失去所有关联关系的实体和段落",
        "hint": "当一个实体或段落的所有关联关系都被剪枝后，它就成为「孤儿节点」。这些节点会经过保留期和宽限期后被物理清理，支持在宽限期内通过 /mem restore 恢复。",
        "items": {
          "enable_soft_delete": {
            "type": "bool",
            "description": "启用软删除回收流程",
            "hint": "开启后，孤儿节点会先被标记为「软删除」状态（可恢复），经过宽限期后才物理删除。关闭则直接永久删除，无法恢复。建议保持开启。",
            "default": true
          },
          "entity_retention_days": {
            "type": "float",
            "description": "孤儿实体保留天数",
            "hint": "实体（如人名、地点、概念等）失去所有关系后，保留多少天才标记为软删除。建议范围 3~30 天。",
            "default": 7.0
          },
          "paragraph_retention_days": {
            "type": "float",
            "description": "孤儿段落保留天数",
            "hint": "段落（具体的记忆内容）失去所有关系后，保留多少天才标记为软删除。建议范围 3~30 天。",
            "default": 7.0
          },
          "sweep_grace_hours": {
            "type": "float",
            "description": "软删除宽限期（小时）",
            "hint": "软删除标记后，等待多少小时才真正物理清理。在此期间可通过 /mem restore 命令恢复。建议范围 12~72 小时。",
            "default": 24.0
          }
        }
      }
    }
  },
  "person_profile": {
    "type": "object",
    "description": "人物画像 —— 自动提取并记录对话中出现的人物信息",
    "hint": "人物画像系统会从消息中提取发送者的特征信息（如兴趣、性格、习惯等），在 LLM 请求时自动注入作为上下文参考，让机器人更了解对话对象。画像支持定期自动刷新，也可手动覆盖和清除。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用人物画像功能",
        "hint": "关闭后将不会生成和注入人物画像信息。如果你不需要机器人记住用户的个人特征，可以关闭以节省 LLM 调用开销。",
        "default": true
      },
      "opt_in_required": {
        "type": "bool",
        "description": "需要用户主动开启画像注入",
        "hint": "开启后，每个用户需要在对应会话中发送 /person_profile on 才会对其注入画像信息。这是一种隐私保护机制，让用户自主选择是否被「记住」。关闭则按 default_injection_enabled 的值决定默认行为。",
        "default": false
      },
      "default_injection_enabled": {
        "type": "bool",
        "description": "未手动设置时的默认画像注入状态",
        "hint": "当用户未通过 /person_profile 命令手动设置开关时，是否默认开启画像注入。opt_in_required=true 时作为“未设置用户”的默认值；opt_in_required=false 时作为全局默认行为。",
        "default": false
      },
      "global_injection_enabled": {
        "type": "bool",
        "description": "启用全局人物画像注入",
        "hint": "开启后将对所有会话强制注入人物画像（仍受 person_profile.enabled 总开关约束），不再要求用户在会话内手动开启。关闭后回退到 opt_in_required/default_injection_enabled 的常规策略。",
        "default": false
      },
      "profile_ttl_minutes": {
        "type": "int",
        "description": "画像缓存有效期（分钟）",
        "hint": "生成的人物画像会缓存指定时长后失效并在下次需要时重新生成。值越大刷新越少（节省 LLM 调用），值越小画像越新。建议范围 60~1440（1 小时到 1 天），默认 360 分钟（6 小时）。",
        "default": 360
      },
      "top_k_evidence": {
        "type": "int",
        "description": "画像生成时参考的证据条数",
        "hint": "生成人物画像时，从记忆库中检索多少条相关记忆作为 LLM 的参考证据。数量越多画像越全面，但也消耗更多 Token。建议范围 5~20。",
        "default": 12
      }
    }
  },
  "schedule": {
    "type": "object",
    "description": "定时总结 —— 自动在每天指定时间对所有会话执行记忆总结",
    "hint": "定时总结会在指定时间点自动触发，对当前作用域内所有有新消息的会话生成摘要并写入记忆系统。适合在低峰期执行，避免影响正常对话响应速度。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用定时自动总结",
        "hint": "开启后，插件会在下方设定的时间点自动执行总结任务。关闭后需手动通过 /mem summary_now 或 /mem summary_all 触发总结。",
        "default": true
      },
      "import_times": {
        "type": "list",
        "description": "每日总结触发时间",
        "hint": "设定每天自动执行总结的时间点，格式为 HH:MM（24 小时制）。支持多个时间点，例如 [\"04:00\", \"16:00\"]。",
        "default": ["04:00"]
      }
    }
  },
  "summarization": {
    "type": "object",
    "description": "总结策略 —— 控制对话总结的数据来源、长度和知识类型",
    "hint": "总结功能会调用 LLM 对一段时间内的对话生成结构化摘要，并将摘要作为高密度知识写入记忆系统。这比直接存储原始对话更高效，能在有限的上下文窗口中携带更多信息。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用总结功能",
        "hint": "关闭后所有总结相关功能（手动/定时/自动）都将停用。记忆系统仍会记录原始消息，但不会生成摘要知识。",
        "default": true
      },
      "source_mode": {
        "type": "string",
        "description": "总结数据来源模式",
        "hint": "transcript：仅使用插件自身记录的消息转录，完全独立于 AstrBot 会话系统；astrbot：仅使用 AstrBot 原生会话历史记录；hybrid（推荐）：优先使用 AstrBot 会话历史，失败时自动回退到插件转录。推荐使用 hybrid 以获得最佳兼容性。",
        "default": "hybrid",
        "options": ["transcript", "astrbot", "hybrid"]
      },
      "chat_provider_id": {
        "type": "string",
        "description": "总结专用聊天 Provider ID",
        "hint": "仅在启用 AstrBot 原生 Provider Bridge 时生效。填写后，总结任务会优先使用这里指定的 Provider，而不会影响日常聊天所用的模型。",
        "default": ""
      },
      "model_name": {
        "type": "string",
        "description": "总结专用模型名",
        "hint": "仅在走 OpenAI-compatible 回退链路时生效。可将“聊天模型”和“总结模型”拆开配置，让总结更稳定、格式更可控。",
        "default": ""
      },
      "context_length": {
        "type": "int",
        "description": "总结上下文消息条数",
        "hint": "每次总结时取最近多少条消息作为 LLM 的输入上下文。数值越大总结越全面但消耗更多 Token。建议范围 20~200，根据你使用的 LLM 上下文窗口大小和费用预算调整。",
        "default": 30
      },
      "default_knowledge_type": {
        "type": "string",
        "description": "总结生成的知识类型",
        "hint": "narrative（叙事型）：按时间线梳理对话脉络，适合日常闲聊；factual（事实型）：提取关键事实和结论，适合信息密度高的对话；mixed（混合型）：同时提取叙事和事实；structured（结构化）：生成结构化的知识条目；auto（自动）：由 LLM 自动判断最适合的类型。",
        "default": "narrative",
        "options": ["narrative", "factual", "mixed", "structured", "auto"]
      },
      "auto_import": {
        "type": "object",
        "description": "自动总结 —— 在对话进行中按条件自动入库",
        "hint": "自动总结采用保守策略：只有新增消息达到阈值、未命中冷却时间、且满足触发时机时才会排队执行，避免每轮回复都触发总结。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用自动总结",
            "hint": "关闭后仅保留手动总结和定时批量总结。",
            "default": true
          },
          "after_reply_only": {
            "type": "bool",
            "description": "仅在成功回复后触发自动总结",
            "hint": "开启后，自动总结只会在机器人完成一次有效回复后检查触发条件；关闭后，用户消息写入后也可参与触发。",
            "default": true
          },
          "min_new_messages": {
            "type": "int",
            "description": "触发自动总结所需的最少新增消息数",
            "hint": "自上次总结后，当前会话至少新增多少条 transcript 消息才允许自动总结。值越大越保守，默认 14。",
            "default": 14
          },
          "cooldown_minutes": {
            "type": "int",
            "description": "自动总结冷却时间（分钟）",
            "hint": "同一会话两次自动总结之间至少间隔多久。用于避免高频聊天反复触发总结，默认 15 分钟。",
            "default": 15
          }
        }
      }
    }
  },
  "webui": {
    "type": "object",
    "description": "内嵌管理界面（WebUI）—— 可视化浏览和管理记忆数据",
    "hint": "Memorix 内置了一个 Web 管理界面，可以可视化浏览知识图谱、管理记忆条目、查看回收站、编辑人物画像等。启动后可通过 /mem ui 获取访问地址。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用 WebUI",
        "hint": "关闭后将不会启动 Web 管理界面。如果不需要可视化管理，可以关闭。",
        "default": true
      },
      "host": {
        "type": "string",
        "description": "WebUI 监听地址",
        "hint": "WebUI 服务器绑定的网络地址。0.0.0.0 表示监听所有网络接口（可从外部访问），127.0.0.1 表示仅本机可访问。",
        "default": "0.0.0.0"
      },
      "port": {
        "type": "int",
        "description": "WebUI 监听端口",
        "hint": "WebUI 服务器使用的端口号。如果端口被占用，插件会自动尝试递增端口号。",
        "default": 8092
      },
      "port_fallback_max_tries": {
        "type": "int",
        "description": "端口冲突时的最大重试次数",
        "hint": "当指定端口被占用时，插件会从该端口开始逐一递增尝试。此值决定最多尝试多少次。",
        "default": 20
      },
      "scope": {
        "type": "string",
        "description": "WebUI 绑定的作用域",
        "hint": "auto（推荐）：WebUI 自动绑定到当前活跃的作用域。也可手动指定作用域标识（如 aiocqhttp）来固定 WebUI 只展示特定作用域的数据。",
        "default": "auto"
      },
      "auth": {
        "type": "object",
        "description": "WebUI 鉴权配置",
        "hint": "为 WebUI 的 API 接口添加鉴权保护。如果 WebUI 暴露在公网或不可信网络中，强烈建议开启鉴权。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用 API 鉴权",
            "hint": "开启后，访问 WebUI 的 API 需要在请求头中携带有效的 Bearer Token。关闭则所有人都可以直接访问。",
            "default": false
          },
          "write_tokens": {
            "type": "list",
            "description": "写操作 Token 列表",
            "hint": "拥有写权限的 Token 列表，持有这些 Token 的请求可以执行删除、修改、导入等写操作。可设置多个 Token 分配给不同的管理者。",
            "default": []
          },
          "read_tokens": {
            "type": "list",
            "description": "读操作 Token 列表",
            "hint": "拥有读权限的 Token 列表，持有这些 Token 的请求只能查看数据。仅在开启 protect_read_endpoints 时需要。",
            "default": []
          },
          "protect_read_endpoints": {
            "type": "bool",
            "description": "对读接口也启用鉴权",
            "hint": "默认情况下鉴权仅保护写接口（增删改），读接口（查询、浏览）不需要 Token。开启后读接口也需要 Token，适合记忆内容涉及敏感信息的场景。",
            "default": false
          }
        }
      }
    }
  },
  "web": {
    "type": "object",
    "description": "导入中心 —— 通过 Web 界面批量导入外部知识到记忆系统",
    "hint": "导入中心提供了一个独立的 Web 页面（/import），支持上传文件（.txt/.md/.json）、粘贴文本和扫描目录三种方式导入外部知识。默认关闭，需手动开启。",
    "items": {
      "import": {
        "type": "object",
        "description": "导入任务配置",
        "hint": "控制导入中心的开关和各项限制。合理的限制可以防止单次导入任务占用过多资源。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用导入中心",
            "hint": "开启后可通过 WebUI 的 /import 页面和 /v1/import/* API 接口导入外部文件和文本到记忆系统。如果不需要导入外部知识，保持关闭即可。",
            "default": false,
            "obvious_hint": true
          },
          "max_queue_size": {
            "type": "int",
            "description": "导入任务队列上限",
            "hint": "同时等待处理的导入任务最大数量。超过上限后新的导入请求会被拒绝，需等待前面的任务完成。建议范围 5~50。",
            "default": 20
          },
          "max_files_per_task": {
            "type": "int",
            "description": "单次导入最大文件数",
            "hint": "单个导入任务中允许包含的最大文件数量。建议范围 10~500。",
            "default": 200
          },
          "max_file_size_mb": {
            "type": "int",
            "description": "单个文件大小上限（MB）",
            "hint": "单个导入文件的最大大小限制，单位为 MB。过大的文件会在解析时消耗大量内存。建议范围 5~100。",
            "default": 20
          },
          "max_paste_chars": {
            "type": "int",
            "description": "粘贴导入最大字符数",
            "hint": "通过粘贴方式导入时，允许的最大文本字符数。建议范围 10000~1000000。",
            "default": 200000
          },
          "default_file_concurrency": {
            "type": "int",
            "description": "文件并发处理数（预留）",
            "hint": "同时处理的文件数量上限。此参数为预留功能，当前版本可保持默认值。",
            "default": 2
          },
          "default_chunk_concurrency": {
            "type": "int",
            "description": "分块并发处理数（预留）",
            "hint": "单个文件拆分为多个块后的并发处理数量。此参数为预留功能，当前版本可保持默认值。",
            "default": 4
          },
          "path_aliases": {
            "type": "object",
            "description": "目录扫描路径别名",
            "hint": "定义导入中心可扫描的目录别名映射。出于安全考虑，导入中心只能访问这里定义的路径别名对应的目录，不允许扫描任意路径。路径相对于插件的数据存储目录。",
            "items": {
              "raw": {
                "type": "string",
                "description": "原始文件目录别名",
                "hint": "对应数据存储目录下的 raw 子目录，可在导入中心中通过 raw 别名访问。可修改为其他子目录名。",
                "default": "raw"
              },
              "plugin_data": {
                "type": "string",
                "description": "插件数据目录别名",
                "hint": "对应数据存储目录的根目录（.）。可修改为其他子目录名以限制扫描范围。",
                "default": "."
              }
            }
          }
        }
      }
    }
  }
}
