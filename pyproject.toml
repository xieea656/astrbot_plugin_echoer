[tool.ruff]
line-length = 120
target-version = "py310"
extend-exclude = [
  "memorix/amemorix",
  "memorix/core",
  "memorix/webui/routes_compat.py",
  "memorix/webui/web",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

